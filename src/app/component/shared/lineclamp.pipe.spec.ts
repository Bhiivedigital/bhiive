import { LineclampPipe } from './lineclamp.pipe';

describe('LineclampPipe', () => {
  it('create an instance', () => {
    const pipe = new LineclampPipe();
    expect(pipe).toBeTruthy();
  });
});
