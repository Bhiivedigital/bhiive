// Production environment.
// cmsUrl must match the deployed Strapi URL. If the Render service ends up
// with a different URL (name taken), update this and rebuild.
export const environment = {
  production: true,
  cmsUrl: 'https://bhiive-cms.onrender.com',
};
